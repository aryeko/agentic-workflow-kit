# Wave 3 outputs

The Wave 3 orchestrator writes here. Expected after a run:

- `T9/proposal.md` (+ `draft/`, optionally split into `T9a`/`T9b`/`T9c` surfaces)
- `WAVE-3-SUMMARY.md` — includes the final "design-closure complete?" verdict.

Wave 3 consumes frozen Wave-1 T3. Proposals only — applying them to the corpus is a separate
architect-approved step.
