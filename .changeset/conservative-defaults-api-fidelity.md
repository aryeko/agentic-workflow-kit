---
"@agentic-workflow-kit/orchestrator": minor
---

Require explicit `--yes` / `confirmNonDryRun` approval before non-dry-run story launches, default workflow initialization to the conservative `push-only` preset, report tracker migration as available, and harden repo-relative path validation.
