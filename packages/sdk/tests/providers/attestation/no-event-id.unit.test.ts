import { describe, expect, it } from 'vitest';

describe('prov-00-s1 event id exclusion', () => {
  it('documents that the event id exclusion is enforced in the tsc fixture suite', () => {
    expect(true).toBe(true);
  });
});
