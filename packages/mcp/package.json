{
  "name": "mcp",
  "version": "0.0.0",
  "description": "MCP server executable skeleton for kit-vnext.",
  "private": true,
  "type": "module",
  "files": [
    "README.md",
    "src"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "provider-codex": "workspace:*",
    "provider-github": "workspace:*",
    "provider-local": "workspace:*",
    "provider-markdown": "workspace:*",
    "sdk": "workspace:*"
  }
}
