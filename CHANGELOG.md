# Changelog

The published package changelog is the release-history source of truth:
[`packages/orchestrator/CHANGELOG.md`](packages/orchestrator/CHANGELOG.md).

agentic-workflow-kit versions the Claude/Codex plugin manifests and the
`@agentic-workflow-kit/orchestrator` package together. The current maintained pre-1.0 line is
`0.5.x`; release preparation and new changesets are handled by the release-readiness workstream.

Keep this root file as a pointer instead of duplicating generated package release entries that can
drift from the package changelog.
