# Check gate

A complete local gate should include:

```txt
format
lint
dependency rules
typecheck
unit tests
integration tests
conformance tests
coverage
```

Smoke tests are separate because they may require real processes, network, credentials, or external services.
