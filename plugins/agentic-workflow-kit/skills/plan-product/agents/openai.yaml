interface:
  display_name: "Plan Product"
  short_description: "Draft a PRD and route next planning step"
  default_prompt: "Use $plan-product to define the product requirements, ask only blocking questions, and recommend the next planning step."

policy:
  allow_implicit_invocation: true
