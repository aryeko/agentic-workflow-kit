interface:
  display_name: "Promote to Canonical"
  short_description: "Fold shipped track knowledge into the canonical knowledge base"
  default_prompt: "Use $promote-to-canonical explicitly after a track ships to update canonical docs, mint ADRs, and archive the PRD."

policy:
  allow_implicit_invocation: false
